# Phase C Replay Mode

Replay Mode allows past tasks to be re-run through the Intuition/Finch runtime and logged into the canonical ledger.
Invocation is explicit; no automatic replays are performed.
