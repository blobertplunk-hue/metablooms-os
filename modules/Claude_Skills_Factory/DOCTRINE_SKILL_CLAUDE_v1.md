# Doctrine: Skills as Progressive Disclosure

Keep the always-on layer small:
- index + manifests + validators
Load deeper skill docs/scripts only when invoked.

This mirrors the “skill.md as table-of-contents” pattern and prevents context bloat while enabling depth.
